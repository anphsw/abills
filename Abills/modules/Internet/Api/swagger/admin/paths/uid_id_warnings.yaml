get:
  tags:
    - internet
  summary: Информация о списаниях
  operationId: userInternetWarnings
  parameters:
    - name: uid
      in: path
      description: UID пользователя
      required: true
      schema:
        type: number
    - name: id
      in: path
      description: ID услуги
      required: true
      schema:
        type: number
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/warning.yaml"
  security:
    - KEY: []