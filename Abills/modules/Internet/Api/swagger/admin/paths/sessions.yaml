get:
  tags:
    - internet
  summary: Получить список интернет сессий
  operationId: getInernetSessions
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/OnlineSessionsArray"
  security:
    - KEY: [ ]