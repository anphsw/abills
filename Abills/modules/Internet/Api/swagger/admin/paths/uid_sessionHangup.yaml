post:
  tags:
    - internet
  summary: Hangup сессии абонента
  parameters:
    - $ref: "#/components/parameters/pathUid"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/addHangupRequest.yaml"
    required: true
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/addHangupResponse200.yaml"
    400:
      description: Валидация не успешна
      content:
        application/json:
          schema:
            $ref: "../schemas/addHangupResponse400.yaml"
  security:
    - KEY: []