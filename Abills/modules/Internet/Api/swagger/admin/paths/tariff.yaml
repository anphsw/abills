post:
  tags:
    - internet
  summary: Добавить тарифный план
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/addTariffRequest.yaml"
    required: true
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/addTariffResponse200.yaml"
    400:
      description: Нет поля ID
      content:
        application/json:
          schema:
            $ref: "../schemas/addTariffResponse400.yaml"
  security:
    - KEY: [ ]