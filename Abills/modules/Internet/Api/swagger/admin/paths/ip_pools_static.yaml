get:
  tags:
    - internet
  summary: Получить список статических IP пулов
  operationId: getInternetIpPoolsStatic
  parameters:
    - $ref: "#/components/parameters/pageRows"
    - $ref: "#/components/parameters/pg"
    - $ref: "#/components/parameters/sort"
    - $ref: "#/components/parameters/desc"
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/ipPoolsList.yaml"
  security:
    - KEY: [ ]

