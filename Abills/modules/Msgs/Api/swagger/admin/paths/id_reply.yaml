get:
  tags:
    - msgs
  summary: Ответы на сообщение
  operationId: repliesListToMsgs
  parameters:
    - name: id
      in: path
      description: ID сообщения
      required: true
      schema:
        type: integer
        format: int64
  responses:
    200:
      description: успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/repliesList.yaml"
  security:
    - KEY: []
post:
  tags:
    - msgs
  summary: Ответить на сообщение
  operationId: addReplyToMsgs
  parameters:
    - name: id
      in: path
      description: ID сообщения
      required: true
      schema:
        type: integer
        format: int64
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/addReplyRequest.yaml"
      multipart/form-data:
        schema:
          allOf:
            - $ref: "../schemas/addReplyRequest.yaml"
            - $ref: "../../user/schemas/msgsFiles.yaml"
    required: true
  responses:
    200:
      description: успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/addReplyResponse200.yaml"
  security:
    - KEY: []