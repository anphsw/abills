get:
  tags:
    - portal
  summary: Рассылка
  parameters:
    - name: id
      in: path
      description: ID вложения
      required: true
      schema:
        type: integer
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/newsletter.yaml"
  security:
    - KEY: [ ]
delete:
  tags:
    - portal
  summary: Удалить рассылку
  parameters:
    - name: id
      in: path
      description: ID рассылки
      required: true
      schema:
        type: integer
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            type: object
            properties:
              result:
                type: string
                example: Successfully deleted
  security:
    - KEY: [ ]