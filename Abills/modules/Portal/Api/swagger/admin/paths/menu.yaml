get:
  tags:
    - portal
  summary: Меню
  parameters:
    - name: id
      in: path
      description: ID меню
      required: true
      schema:
        type: integer
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/menu.yaml"
  security:
    - KEY: [ ]
put:
  tags:
    - portal
  summary: Изменить меню
  parameters:
    - name: id
      in: path
      description: ID меню
      required: true
      schema:
        type: integer
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/menu.yaml"
    required: true
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/menuChangeResponse.yaml"
delete:
  tags:
    - portal
  summary: Удалить меню
  parameters:
    - name: id
      in: path
      description: ID меню
      required: true
      schema:
        type: integer
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            type: object
            properties:
              result:
                type: string
                example: Successfully deleted
  security:
    - KEY: [ ]