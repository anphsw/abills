post:
  tags:
    - equipment
  summary: Добавить информацию о положении оборудования на карте сети
  operationId: nasAddNetmapPosition
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/addNasNetmapPosition.yaml"
    required: true
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiAddResponse200"
    400:
      description: Дубликат
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiAddResponse400"
  security:
    - KEY: []