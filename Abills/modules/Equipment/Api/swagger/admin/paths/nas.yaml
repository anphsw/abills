post:
  tags:
    - equipment
  summary: Добавить сервер доступа
  operationId: nasAdd
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/addNasRequest.yaml"
    required: true
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/addNasResponse200.yaml"
    400:
      description: Нет поля ip
      content:
        application/json:
          schema:
            $ref: "../schemas/addNasResponse400.yaml"
  security:
    - KEY: []