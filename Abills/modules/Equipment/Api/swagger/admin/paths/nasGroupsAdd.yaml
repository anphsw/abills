post:
  tags:
    - equipment
  summary: Добавить группу серверов доступа
  operationId: nasGroupAdd
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/addNasGroupRequest.yaml"
    required: true
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiAddResponse200"
    400:
      description: Дубликат названия
      content:
        application/json:
          schema:
            $ref: "../schemas/addNasGroupResponse400.yaml"
  security:
    - KEY: []