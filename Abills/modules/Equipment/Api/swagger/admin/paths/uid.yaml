get:
  tags:
    - equipment
  summary: Список оборудования в пользование
  parameters:
    - $ref: "#/components/parameters/pathUid"
  operationId: equipmentUserDevices
  responses:
    200:
      description: успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/userDevices.yaml"
  security:
    - KEY: [ ]