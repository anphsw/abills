get:
  tags:
    - ureports
  summary: Список плагинов
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/plugins.yaml"
  security:
    - KEY: [ ]