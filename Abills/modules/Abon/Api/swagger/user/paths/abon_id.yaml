post:
  tags:
    - abon
  summary: Активация сервиса
  parameters:
    - name: id
      in: path
      required: true
      description: ID сервиса
      schema:
        type: integer
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/activateServiceResponse200.yaml"
    400:
      description: Слишком маленький депозит
      content:
        application/json:
          schema:
            $ref: "../schemas/activateServiceResponse400.yaml"
  security:
    - USERSID: [ ]