get:
  tags:
    - paysys
  summary: Получить информацию о регулярном платеже
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/paysysRecurrentInfo200.yaml"
    400:
      description: Нет подписки
      content:
        application/json:
          schema:
            $ref: "../schemas/paysysRecurrentInfo400.yaml"
  security:
    - USERSID: [ ]
delete:
  tags:
    - paysys
  summary: Удалаить регулярный платеж
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/paysysRecurrentDelete200.yaml"
    400:
      description: Нет регулярного платежа
      content:
        application/json:
          schema:
            $ref: "../schemas/paysysRecurrentDelete200.yaml"
  security:
    - USERSID: [ ]