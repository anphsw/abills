post:
  tags:
    - voip
  summary: Добавить тарификацию
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/addExtraTarificationRequest.yaml"
    required: true
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiAddResponse200"
    400:
      description: Дубликат
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiAddResponse400"
  security:
    - KEY: []