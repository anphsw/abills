post:
  tags:
    - fees
  summary: Добавление списания пользователю
  operationId: addUserFeeOld
  deprecated: true
  parameters:
    - $ref: "#/components/parameters/pathUid"
    - name: sum
      in: path
      description: Сумма списания
      required: true
      schema:
        type: integer
        format: int64
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/userFeeAddOld.yaml"
    required: true
  responses:
    400:
      description: Invalid ID supplied
      content: {}
  security:
    - KEY: []