post:
  tags:
    - admins
  summary: Изменить права администратора
  operationId: adminSetPermissions
  parameters:
    - $ref: "#/components/parameters/pathAid"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/setPermissionsRequest.yaml"
    required: true
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/setPermissionsResponse.yaml"
  security:
    - KEY: []