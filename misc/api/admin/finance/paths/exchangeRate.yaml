get:
  tags:
    - finance
  summary: Получить список обменов валют
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/rates.yaml"
  security:
    - KEY: [ ]