get:
  tags:
    - finance
  summary: Получить список истории обменов валют
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/ratesLog.yaml"
  security:
    - KEY: [ ]