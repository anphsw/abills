post:
  tags:
    - user
  summary: Восстановление пароля
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/passwordRecoveryRequest.yaml"
    required: true
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/passwordRecoveryResponse200.yaml"
    400:
      description: Нет ни login, uid или contractId
      content:
        application/json:
          schema:
            $ref: "../schemas/passwordRecoveryResponse400.yaml"