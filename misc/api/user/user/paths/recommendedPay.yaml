get:
  tags:
    - user
  summary: Рекомендуемая сумма к оплате
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/recommendedPay.yaml"
  security:
    - USERSID: [ ]