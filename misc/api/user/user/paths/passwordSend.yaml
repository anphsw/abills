post:
  tags:
    - user
  summary: Получить пароль
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/passwordSendResponse200.yaml"
    400:
      description: Не включена опция USER_SEND_PASSWORD
      content:
        application/json:
          schema:
            $ref: "../schemas/passwordSendResponse400.yaml"
  security:
    - USERSID: [ ]