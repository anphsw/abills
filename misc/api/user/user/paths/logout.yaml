delete:
  tags:
    - user
  summary: Выход пользователя
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/responseOk.yaml"
  security:
    - USERSID: [ ]
