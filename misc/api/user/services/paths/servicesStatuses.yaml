get:
  tags:
    - services
  summary: Список всех статусов услуг
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/servicesStatuses.yaml"
  security:
    - USERSID: [ ]